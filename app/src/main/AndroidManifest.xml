<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Gates AppUpdateInstaller.canInstallPackages()/installPackage() (the Settings screen's
         "Update to latest" button) - granting this is a special, per-app "install unknown apps"
         toggle the user must flip in system Settings; the permission's mere presence here does
         not enable sideloading by itself, it only lets the app ask the OS whether it's allowed. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".CompareApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Compare App"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Receive shared/tapped locations, e.g. "Open with" from Maps -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="geo" />
            </intent-filter>

            <!-- Receive a target location directly from another app -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="compareapp" android:host="dropoff" />
            </intent-filter>

            <!-- Appear as a share target for Google Maps' "Share" button, which sends the
                 place as ACTION_SEND text/plain (e.g. a maps.app.goo.gl link), not ACTION_VIEW. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Lets AppUpdateInstaller hand a downloaded update APK (cached under
             context.cacheDir/updates/ by UpdateRepositoryImpl) to the system Package Installer
             as a content:// Uri - a plain file:// Uri would throw FileUriExposedException under
             this app's targetSdk. The authority here must match AppUpdateInstaller's
             FILE_PROVIDER_AUTHORITY_SUFFIX exactly, and update_file_paths.xml's cache-path must
             match UpdateRepositoryImpl's download directory exactly. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.update.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_file_paths" />
        </provider>

        <!-- Deletes the APK UpdateRepositoryImpl downloaded to context.cacheDir once this app
             finishes installing it, so it doesn't sit there wasting space. MY_PACKAGE_REPLACED is
             one of the few broadcasts exempted from Android 8+'s implicit-broadcast background
             restrictions, and is only delivered to the app that was just updated - see
             UpdateApkCleanupReceiver. Must be manifest-declared (not registered at runtime) since
             the app process is commonly killed during its own update install. -->
        <receiver
            android:name=".receiver.UpdateApkCleanupReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
    <!-- Package visibility (API 30+): without a <queries> entry, PackageManager.getPackageInfo()
         (AppRepository.isAppInstalled) throws NameNotFoundException for any package not listed
         here - not just implicit intent resolution - so every app this screen checks/targets by
         package (ride comparison + food search) needs an entry, or "app not installed" warnings
         become permanently, silently wrong on modern Android regardless of what's actually
         installed. See FoodDeliveryProvider.kt for the food package names. -->
    <queries>
        <package android:name="com.ubercab" />
        <package android:name="ee.mtakso.client" />
        <package android:name="com.ubercab.eats" />
        <package android:name="com.bolt.deliveryclient" />
        <package android:name="com.glovo" />
    </queries>
</manifest>
